<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <h1>EDIT USER</h1>

    <form class="" action="/users/{{user._id}}" method="post">
      <div class="">
        <label for="name">Name</label>
        <input type="text" name="name" value="{{user.name}}">{{#errors.name}}Name is required{{/errors.name}}
      </div>

      <div class="">
        <label for="state">userName</label>
        <input type="text" name="userName" value="{{user.userName}}">{{#errors.state}}State is required{{/errors.state}}
      </div>

      <button type="submit" name="button">Update Profile</button>
    </form>


  </body>
</html>
